import { makeStyles } from "@material-ui/core";

export const useAppStyles = makeStyles({
  grid: {
    display: "flex",
    flexWrap: "wrap",
    flexDirection: "row",
    justifyContent: "center"
  }
});
